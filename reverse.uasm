| Lugod, Waleed
| Sagusay, Joseph
| Warain, John Victor

| 2. Reverse the elements of an array
.include beta.uasm

        ADDC(r31, DATA, r0)     | r0 <--- left pointer; r1 <--- right pointer
        ADDC(r0, 4, r0)
        LD(r31, DATA, r1)       | Initially, the left and right pointer point to 
        SHLC(r1, 2, r1)         | the first and last element respectively.
        ADDC(r1, DATA, r1)         | 

LOOP:
        CMPLT(r0, r1, r3)       | Left and right pointers will converge to the middle element.
        BEQ(r3, END, r31)       | Once left pointer > right pointer, the loop ends.

        LD(r0, 0, r4)
        LD(r1, 0, r5)
        ST(r5, 0, r0)
        ST(r4, 0, r1)

        ADDC(r0, 4, r0)
        SUBC(r1, 4, r1)
        BR(LOOP)

END:
        HALT()

.=0x200
DATA:   LONG(5) 
        LONG(3)
        LONG(-5)
        LONG(12)
        LONG(-14)
        LONG(1)